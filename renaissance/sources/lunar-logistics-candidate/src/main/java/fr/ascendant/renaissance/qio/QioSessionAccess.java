package fr.ascendant.renaissance.qio;

/** Implemented only by the viewer mixin, never by a shared block holder. */
public interface QioSessionAccess {
    QioManualSession renaissance$qioSession();
}
